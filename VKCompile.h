#pragma once

/**
@brief 
* "VulkanSDK/バージョン/glslang/CMakeList.txt" を使って cmake でプロジェクトを生成する
* glslang.sln を開いて glslang, glslangValidator, OGLCompipler, OSDependent, SPIRV, spirv - remap をビルドする
(64bitで使う場合は x64 を追加してビルドしないとだめみたい)
*/
#include <../glslang/SPIRV/GlslangToSpv.h>

