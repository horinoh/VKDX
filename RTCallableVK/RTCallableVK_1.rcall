#version 460
#extension GL_EXT_ray_tracing : enable

layout(location = 0) callableDataInEXT vec3 CallableData;

void main()
{
	//!< cü
	const vec2 pos = vec2(gl_LaunchIDEXT.xy / 8);
	CallableData = vec3(mod(pos.x, 2.0f));
}